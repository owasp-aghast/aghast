# Claude Code
.claude/

# Node.js
node_modules/
dist/

# Test output
test-results.xml
.tmp-test-*

# Environment files (contain secrets)
.env
.env.local
.env.*.local

# Worktrees
.worktrees/

# Skill temp files
.claude-tmp/

.vscode

# Other repos (cloned as subfolders)
checks-config/
checks-config-public/

# Test codebases (live in external repo)
test-codebases/

# Local scripts
*.ps1
# Scan output — written into the target repo by default, so a scan run from the
# repo root (or against a test fixture) drops one here. Easy to commit by
# accident; see PR #258 which needed a follow-up commit to remove one.
security_checks_results.*
